---
// Editor panel — textarea only (Toolbar is rendered separately in index.astro)
---

<div id="editor-panel" class="flex h-full flex-col bg-[var(--color-bg-primary)]">
  <textarea
    id="markdown-input"
    class="flex-1 resize-none overflow-y-auto border-none bg-[var(--color-bg-primary)] p-4 pb-20 font-mono text-sm leading-relaxed text-[var(--color-text-primary)] outline-none placeholder:text-[var(--color-text-secondary)] md:pb-4"
    placeholder="Write your Markdown here..."
    spellcheck="false"></textarea>
</div>
